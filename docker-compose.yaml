version: "3.9"

services:
  book-management-system:
    build: .
    image: abhishek1979/book-management-system:v1
    container_name: book-management-system

    ports:
      - "9999:9999"

    volumes:
      - .:/book-management-system

    restart: always